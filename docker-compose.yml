version: '3.9'
services:
  app:
    build: ./app
    restart: always
    environment:
      PYTHONUNBUFFERED: 1
      API_URL: ${API_URL}
      ADMIN_SECRET: ${ADMIN_SECRET}
      INTERVAL: ${INTERVAL}
    devices:
      - '${HOST_PORT:-/dev/ttyUSB0}:/dev/ttyUSB0'
